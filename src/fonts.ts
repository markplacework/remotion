// Bold, widely-available system stack. No network fetch is involved, so
// this renders identically in Remotion Studio and in headless rendering
// without depending on external font CDNs being reachable.
export const wapiFont =
  '"Helvetica Neue", Helvetica, Arial, sans-serif';
